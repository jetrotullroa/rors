<div class="session-form">
  <h2 class="text-center">Register</h2>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, autofocus: true, label: "Email *" %>
    <%= f.input :password, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length), label: "Password *" %>
    <%= f.input :password_confirmation, label: "Confirm Password *" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Register", class: "btn btn-block worqawt-button" %>
  </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
